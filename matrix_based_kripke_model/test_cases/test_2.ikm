// In this example we are simulating the use of this model for type checking
// P and Q (and other vars so on) are arbitrary variables that are either initialised or not.
// With the expr with can check whether a they are defined in a specific way
// thoughout the abstract 'snapshots' of the evolution of the code execution.

worlds {
    w0: P=0, Q=0
    w1: P=1, Q=0
    w2: P=1, Q=1, Z=0
    w3: P=1, Q=1, Z=1
}
access {
    w0: w0, w1, w2, w3
    w1: w1, w2, w3
    w2: w2, w3
    w3: w3
}
expr: (#P && #Q) -> Z